# Production environment variables for Electron build
# This file is used during the build process to configure the API base URL

# API Base URL for production Electron app
# The server runs on localhost:3002 in the packaged app
VITE_API_BASE_URL=http://localhost:3002/api

# Gemini API Key (should be set during build or runtime)
# GEMINI_API_KEY=your_api_key_here